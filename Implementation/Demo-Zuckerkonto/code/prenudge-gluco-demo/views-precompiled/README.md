To update a precombiled view:

```
handlebars transactions.handlebars -f transactions.precompiled.js
```

The precompiled view is automatically served statically. The compiling could be done automatically, but since we only
have one template...